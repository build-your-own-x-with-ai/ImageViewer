# 更新日志

本文件记录每个版本**新增了什么能力**，而不是改了哪些文件 —— 后者 git log 里
已经有了，抄一遍没有意义。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

阶段 4：WebP。

## [0.3.0] — 2026-09-15

第五种格式：JPEG。3390 行新代码里 3351 行在 `codecs/jpeg/` 里面 —— 和上个
版本恰好相反，JPEG 不把复杂度外包给任何人，它自己就是那个复杂的东西。

难点也不在行数上。前四种格式都能写成流水线「读头部 → 读像素」，JPEG 不能：
DQT/DHT 是**可变的当前状态**（同号后来的覆盖先前的）、熵数据**没有长度
字段**（扫描自己决定在哪结束）、SOS 可以出现多次（渐进模式下一个块要被写
好几遍）。所以解码器是台状态机，`jpeg_scan.dart` 一个文件 678 行。

### JPEG

- **marker 层** —— `FF` + 类型 + 2 字节长度（长度含自身）。marker 前允许
  任意多个 `FF` 填充，所以「找下一个 marker」不是读两个字节。熵数据里的
  `FF` 用 `FF 00` 转义 —— 这也是霍夫曼表**不许分配全 1 码字**的原因：
  少一个全 1 码字，就少一批 `FF` 要转义。
- **13 个 SOFn 里支持 3 个** —— SOF0 基线、SOF1 扩展顺序、SOF2 渐进。
  SOF1 和 SOF0 的解码路径完全一样（8 位 2 表的子集一字不差），所以只当标签
  存着不分叉。无损（SOF3）、层次（SOF5–7）、算术编码（SOF9–11）抛
  `UnsupportedImageFeature`。
- **霍夫曼解码** —— 和 deflate 同一个思想（规范化 Huffman），但位序相反、
  最大码长 16、码表传的是 `BITS[1..16]` + `HUFFVAL`。JPEG 直接传了构造
  码表需要的那两个平坦数组，连转换都不用做。**过订阅拒绝，不完整接受** ——
  规范自己就要求留全 1 码字这个空洞，「表不完整」在这里是正常状态。
- **反量化 + zigzag 反序** —— 一个 DQT 段可带多张表，`Pq` 决定 8 位还是
  16 位。zigzag 让量化后的高频零聚到尾部，被 EOB 一个符号带过去。
- **两套 IDCT** —— `IdctNaive` 照抄规范 A.3.3 的二维求和式（一个块 4096 次
  乘加），`IdctFast` 行列分离的整数蝶形（约 200 次乘法）。留着慢的那个是
  为了让「快版本写错了」变成一条断言：随机系数跑上百个块，两者差不超过 1。
- **四种抽样 4:4:4 / 4:2:2 / 4:2:0 / 4:1:1** —— 倍数由 `maxH/h` 反算，所以
  是**相对**的（`Y=4x4 / C=2x2` 和 `Y=2x2 / C=1x1` 都是 2 倍），非整数倍
  也吃得下。
- **色度上采样** —— 三角滤波（`3/4 近 + 1/4 远`）用于 h2v1 和 h2v2，其余
  倍数退回最近邻，照抄 libjpeg 的分派。边界一律夹在**真实样本数**上，不是
  平面宽度 —— 平面右边挂着 MCU 补齐的 dummy block，照平面宽度夹会把垃圾拌
  进最后几列色度，症状是右边缘一条彩边。
- **渐进模式四条路径** —— DC 首趟 / DC 细化 / AC 首趟 / AC 细化。细化趟读
  **裸位、不过霍夫曼表**，和首趟几乎没有共用代码。AC 扫描的 `EOBn` 是
  **跨块**游程（「接下来 n 个块在这一频段全是零」），和基线只结束当前块的
  EOB 不是一回事 —— 忘了这层会解出规律性的方格噪声。
- **重启间隔** —— 每 Ri 个单元一个 `RSTn`，marker 号在 `D0..D7` 循环。
  「单元」在交错扫描里是 MCU、在非交错扫描里是**块**，而非交错走的是真实
  块网格而不是 MCU 补齐后的网格 —— 这是两套块数唯一分岔的地方。
- **色彩空间推断** —— JPEG 从不声明自己是什么颜色，只能靠分量数 + APP0/APP14
  + 分量 ID 推。照抄 libjpeg 的 `default_decompress_parms`，其中
  **JFIF 的优先级高于 `'R''G''B'` 分量 ID**：有编码器两个都写，反过来判会把
  正常的 YCbCr 图当 RGB 解。
- **Adobe CMYK / YCCK** —— APP14 的 `transform` 在载荷第 11 字节（flags0 和
  flags1 各占 2 字节）。APP14 存在就意味着样本**取反存储**（255 = 无墨），
  只作用于四分量路径，RGB 那条必须原样透传。
- **APP1 EXIF 方向** —— 载荷是套在 JPEG 里的一个完整 TIFF：字节序运行时才
  知道（`'II'`/`'MM'`，全项目唯一需要动态字节序的地方）、偏移相对 TIFF 头
  而非文件头、内联值**左对齐**。八种取值就是正方形的对称群，分解成
  `flipX`/`flipY`/`swapsDimensions` 三个布尔量而不是写八个分支。畸形 EXIF
  一律返回 null 而不抛异常 —— 相机固件写坏它不罕见，为此拒绝打开一张本来能
  解的图不划算。

### 测试

731 个测试（阶段 2 是 496）。core 68、compress 67、BMP 76、PNG 97、
**JPEG 223**、PNM 38、YUV 64、样图 89、UI 9。

有损格式的测试数据没法像前四种那样手写像素，所以分两条路：

- **单元测试**靠 `test/support/jpeg_builders.dart`（463 行）里的一个小编码器。
  它只编平坦色块：DC-only 的块 IDCT 出常数 `DC/8 + 128`，于是
  `dcCoefficient = (sample - 128) * 8` 能渲出一个**精确已知**的样本值。
  流水线任何一环错了结果都会变 —— 量化表接错则亮度偏、MCU 顺序错则分量串色、
  上采样错则边缘破、重启 marker 位置错则后半张图错位。
- **交叉验证**拿 libjpeg-turbo 当参照。`tool/gen_jpeg_samples.sh` 用 `cjpeg`
  生成六张样图，再用 `djpeg -pnm` 生成五份参考解码结果存进
  `test/assets/expected/`。测试进程仍然从不 shell out。

六张新样图各钉一处：`gradient_64x48_q90_444.jpg`（无子采样，纯 IDCT 精度）、
`gradient_64x48_q75_420.jpg`（4:2:0 + 三角滤波，**逐字节相等**）、
`gradient_64x48_q80_422rst.jpg`（4:2:2 + 重启 marker）、
`gradient_64x48_q80_prog.jpg`（渐进 10 趟，四条路径全覆盖）、
`gradient_64x32_q85_gray.jpg`（灰度，无颜色转换无上采样，**逐字节相等**）、
`gradient_64x48_exif6.jpg`（方向 6，48×64）。

每张同时比**两个**参照：`djpeg` 的输出，和压缩前的原图。两个都需要 ——
我们和 libjpeg 同属 islow IDCT 家族，有可能一起错；而量化损失把源图比对的
容差推到 8，单靠它抓不住多少东西。

`docs/formats/jpeg.md` 结尾那段 482 字节灰度图的逐字节标注里，第一个 DC 系数
手算出来是 -182，反量化后整块重建成 14.25 —— 和 `djpeg` 输出的第 0 行前 8 个
像素的均值**恰好相等**。这段标注由「文档里那张 482 字节的灰度图」一组（5 个
用例）钉住：总长、各段偏移（并要求长度字段能一段段链得上）、SOF0 的 9 个载荷
字节、量化表第 0 项，以及从熵数据的位一路手算到 14.25 并与解码器的实际输出
对齐。png.md 和 yuv.md 的字节级实例各有同样用途的一组 —— 三种格式的转储都
不会悄悄和代码脱节。

### 修复

以下都是本阶段自己写出来又自己抓到的，记下来因为症状都很有欺骗性：

- `_decodeDcRefine` 只看 `scan.components[0]`。渐进 DC 细化趟是唯一允许交错的
  渐进扫描，所以彩色图的 Cb/Cr 细化位会被丢掉 —— 症状是色度略偏，灰度图完全
  正常。
- `JpegOrientation` 里方向 6 和 8 的 `flipX`/`flipY` 写反了。两者都是转 90°，
  只差方向，写反了图能出来、尺寸也对，只是转错了边。
- `samplingLabel` 没做 gcd 约简。抽样因子是**相对**的，`Y=4x4 / C=2x2` 和
  `Y=2x2 / C=1x1` 是同一种 4:2:0，但前者会认不出来、退回成原始因子列表
  `4x4 2x2 2x2`。
- `parseSof` 绕过已解析的分量对象去原始字节里取 `quantTableId`。
- `_decodeAcRefine` 每个符号重查一次 `_acTable`。
- `jpeg_idct.dart` 类文档里两个数写错了（定点增益 30606、列变换中间值上界
  4096）—— 文档里的数字算不对，读者只会以为自己错了。

还有一处不是本阶段写的，是本阶段找文档引用时撞上的：

- `test/codecs/png_test.dart` 里有一个**字面的 NUL 字节**（阶段 2 留下的，本该
  写成 `\x00`）。Dart 编译器不在乎，1953 行的测试一直全绿；但 `grep` 会把整个
  文件判成二进制，然后**一声不响地什么都不返回**。差点因此得出「png.md 声称的
  那一组测试不存在」的结论。改成转义序列，字符串的值一个比特没变。顺手扫了
  全仓库的 `.dart`/`.md`/`.yaml`/`.sh`/`.json`，没有别处有这个问题。

### 已知限制

- **算术编码、无损、层次/差分模式**都抛 `UnsupportedImageFeature`。算术编码
  比霍夫曼高 5~10% 压缩率，但专利到期得晚，实际产出的编码器几乎没有。
- **12 位精度**没做。SOF1 允许，但需要另一套量化和 IDCT 值域 —— 和 16 位 PNG
  是同一个障碍（`RgbaImage` 是 RGBA8888）。
- **CMYK / YCCK 没有交叉验证参照**。`djpeg` 的 PNM 输出只支持 1 或 3 分量，
  四分量图它写不出来，那两条路径只有单元测试覆盖。
- **EXIF 只解方向标记**（0x0112）。相机型号、光圈、GPS 都在同一个 IFD 里，
  不进信息面板。ICC（APP2 分片）同理，一律按 sRGB 显示。
- **DNL 段**（`Y=0` 时的动态高度）直接拒 —— 没有编码器这么写。
- EXIF 缩略图、APP0 内嵌缩略图都跳过，只解主图。

## [0.2.0] — 2026-09-10

第四种格式：PNG。但这个版本一多半的代码不是 PNG，而是 **deflate** ——
PNG 自己的语义只有 IHDR 十三个字节加五个滤波器，复杂度全外包给了压缩层。

### compress 层（新）

- **`inflate` 自写** —— stored / 固定 Huffman / 动态 Huffman 三种块类型，
  完整的 zlib 容器校验（CMF/FLG 的 `% 31`、CM 必须为 8、PNG 禁用 FDICT）。
- **规范化 Huffman 解码** —— 只用两个扁平数组（各码长的计数与符号表），
  不建树。附 Kraft 不等式检查：过订阅一律拒绝，不完整仅在符号数 ≤ 1 时
  放行（与 zlib 行为一致）。
- **LZ77 回溯**，最大距离 32768。重叠拷贝逐字节进行 —— 距离 1、长度 100
  的意思是「把一个字节重复 100 遍」，按 memcpy 语义实现会解错。
- **解压炸弹防护** —— `sizeLimit` 在写入前拦截，而不是解完再比大小。
  PNG 把自己算出来的精确 `rawSize` 同时当作 `sizeHint` 和 `sizeLimit`。
- **Adler-32** —— 校验解压**产物**。这是自写解压器最值钱的一道防线：
  CRC-32 校验的是压缩前的字节，解压器有 bug 时它会全部通过（见
  `implementation.md` 第 3.10 节，这件事真的发生过）。

`compress/` 放在 `codecs/` 之下、不知道「PNG」这个词的存在 —— 阶段 4 的
WebP VP8L 与 ALPH 会复用它。

### PNG

- **chunk 层** —— 长度/类型/数据/CRC-32 四段结构，自建 `0xEDB88320` 查表。
  首字母大小写位决定「不认识的 chunk 该报错还是该跳过」，所以
  `gAMA`/`pHYs`/`tEXt` 这些辅助 chunk 读出来显示，不影响解码。
- **八字节签名** —— `89 50 4E 47 0D 0A 1A 0A`。每个字节各防一种传输事故，
  其中字节 4–5 和 7 合起来能识别四种不同的换行符转换。
- **五种滤波器逆运算** —— None / Sub / Up / Average / Paeth。越界邻居取 0，
  所以第 0 行不需要特例，Paeth 在那里自动退化成 Sub。
- **十五种色彩类型 × 位深组合** —— 0/2/3/4/6 × 1/2/4/8/16 的全部合法组合
  （不是 5 × 5：只有灰度支持五种位深）。走「统一取采样 + 按类型组装」
  两步，而不是十五份手写循环。
- **`tRNS` 透明** —— 三种格式随色彩类型而变：调色板逐项 alpha、灰度键色、
  RGB 键色。键色比较必须用**缩放前**的原始采样。
- **Adam7 隔行** —— 七遍扫描，每遍的行宽要重算，零宽/零高的遍**一个字节都
  不占**（连滤波字节都没有）。逐遍补齐使隔行的原始数据反而更大：
  64×64 灰度下 12408 对 12352 字节。
- **16 位降 8 位** —— 四种位深的缩放系数（×255 / ×85 / ×17 / 四舍五入除法）。
  调色板索引是唯一绝不能缩放的采样。
- **顺序约束** —— IHDR 必须在首、IEND 必须在尾、PLTE 先于 IDAT、
  多个 IDAT 必须连续。最后一条是必须校验的：一个 deflate 块可以跨 IDAT
  边界，所以要先把所有 IDAT 拼起来再解压。

### 测试

496 个测试（阶段 1 是 305）。core 68、compress 67、BMP 76、**PNG 97**、
PNM 38、YUV 64、样图 77、UI 9。

六张新样图，每张针对一处具体陷阱：`rings_64x64_adam7.png`（七遍扫描）、
`ramp_64x64_gray16.png`（16 位缩放的取整方向）、`checker_35x24_gray1.png`
（1 位深 + 非 8 整数倍宽度的行内补位）、`hues_64x48_palette8.png`（调色板）、
`disc_64x64_rgba8.png`（真 alpha）、`gradient_96x64_rgb8.png`（滤波器切换）。

`docs/formats/png.md` 结尾那段 82 字节的十六进制转储被钉进了测试 ——
文档里的每个字节都要真能解出预期结果，否则测试失败。

### 修复

- `huffman.dart` 建表时的下标差一（`offsets[len + 1]++` 应为
  `offsets[len]++`）。症状是解压不报错、Adler-32 报错。
- 两处 `<< 24` 在 Web 上会得到负数（Dart 的位运算在 JS 上是 32 位**有符号**
  的）：`inflate.dart` 读 Adler-32、`rgba_image.dart` 的 `pixelAt`。
  改用乘法。`flutter test` 跑在 VM 上，这类 bug 一个测试都拦不住。

### 已知限制

- APNG 没做。`acTL`/`fcTL`/`fdAT` 都是辅助 chunk，所以打开一个 APNG 会
  正常显示它的第一帧，不报错 —— 这是规范设计好的向后兼容行为。
- 16 位 PNG 一律降到 8 位（`RgbaImage` 是 RGBA8888）。
- `gAMA` 只在信息面板里显示，不参与渲染。

## [0.1.0] — 2026-09-07

第一个能跑起来的版本：三种格式的解码器 + 六平台可编译的 UI 外壳。

### 解码器

- **BMP** —— 六个头部版本（CORE/INFO/V2/V3/V4/V5）、1/2/4/8 bpp 调色板、
  16 bpp（RGB555/RGB565/4-4-4-4/任意 `BI_BITFIELDS` 掩码）、24/32 bpp、
  RLE4/RLE8 游程解码、自底向上与自顶向下、行 4 字节对齐。
- **PNM** —— P1–P6 六种签名，ASCII 与二进制两路，maxval 1–65535
  （含 16 位降 8 位），注释与任意空白的词法处理。
- **裸 YUV** —— 九种像素布局（I420/YV12/I422/I444/NV12/NV21/YUY2/YVYU/UYVY）
  × BT.601/709/2020 × limited/full range，多帧序列，奇数尺寸色度平面
  向上取整 + 最近邻上采样。

三个解码器都是手写的字节解析，**不依赖任何图像解码库** —— 这是本项目的
全部意义所在。`pubspec.yaml` 里除了 `flutter_lints` 和 `cupertino_icons`
没有别的东西。

### core 层

- `ByteReader` —— 大小端读取，每次读取前做边界检查。
- `BitReaderMsb` / `BitReaderLsb` —— 两个方向的位流读取器。JPEG 走 MSB
  （含 `0xFF00` 字节填充处理），deflate 与 VP8L 走 LSB。
- `RgbaImage` —— 所有格式归一到 RGBA8888 的统一容器，带可展示的元数据。
- `DecoderRegistry` —— 按魔数嗅探分派，**从不看扩展名**。

### UI 外壳

- 缩放平移视图。放大时用最近邻、缩小时用线性插值 —— 前者保证一个图像
  像素是一个实心方块（解码错位才看得见），后者避免缩小时的摩尔纹。
- 三条并存的文件来源：系统选择器（各平台）、目录浏览侧栏（桌面）、
  内置样图（各平台，保证空手启动也有东西看）。
- 格式信息面板。遍历 `metadata.toDisplayMap()` 渲染，以后加 PNG/JPEG/WebP
  不需要改 UI 代码。
- YUV 参数对话框。实时显示这组参数把文件切成几帧、余多少字节 ——
  余数不为 0 几乎必定是参数填错了，而**解码器本身发现不了这件事**
  （字节数够它就照解，画出一张斜图还不报错）。
- 错误提示按四类异常分开给建议：没人认领（可能是裸流）、认领了但数据坏了
  （给出字节偏移）、格式合法但特性未实现、连文件都没读到（权限问题）。
- `Isolate.run` 后台解码，Web 上回退到同步路径。

### 测试

305 个测试。core 层 67、BMP 76、PNM 38、YUV 64、样图 51、widget 9。

十二张内置样图由 `tool/gen_samples.dart` 生成，每一张都针对一个具体的
解码器陷阱（行对齐、自顶向下、RLE 增量跳转、YUV 尺寸错配……），
`test/assets/samples_test.dart` 逐张验证它们确实还在踩那个陷阱。

### 已知限制

- macOS 沙盒下只能读用户经由系统对话框明确选中的路径。这是系统的设计，
  不是绕过去的对象 —— 侧栏因此用「选择文件夹」而不是直接列 `~/Pictures`。
- 窄窗口下侧栏收进抽屉，抽屉一关会丢掉当前浏览的目录。
